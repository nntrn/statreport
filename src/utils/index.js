const createAndWrite = require('./createAndWrite')
const parseArgs = require('./parseArgs')
const removeTimeFromDate = require('./removeTimeFromDate')

module.exports = {
  createAndWrite,
  parseArgs,
  removeTimeFromDate
}
